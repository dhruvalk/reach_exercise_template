import React from "react";
import Card from "./Card";
import styles from "./RecycleInfo.module.css";

export default function RecycleInfo() {
  return <Card className={styles.card}>This is the steps to recycle card</Card>;
}
